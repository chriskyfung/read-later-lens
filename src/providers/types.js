/**
 * @fileoverview Minimal type definitions shared across the providers layer.
 *
 * Kept as JSDoc so the rest of the app (plain JS) can reference a provider
 * slug without importing a runtime module.
 */

/**
 * @typedef {'instapaper' | string} ProviderSlug
 */

export {};
